import { Button, Container } from '../styles/style'

function Header() {
  return (
    <Container bgColor="pink">
      <Button background="purple">Header button</Button>
    </Container>
  )
}

export default Header
