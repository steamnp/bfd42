import React from "react";
import ReactDOM from "react-dom/client";
import Home from "./utils/pages/home/home.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>
);
